[安装说明]
把ThinkPHP、Admin、HOME、Public目录直接上传（或拷贝到）服务器web目录下面
导入admin.sql数据库脚本到mysql数据库
（如果是mysql4.1以下版本，请修改admin.sql文件，把DEFAULT CHARSET=utf8去掉）
注意示例程序mysql保存数据采用utf-8编码，请注意mysql相应设置。
修改配置文件config.php设置好数据库访问信息
如果是Unix类环境，请保证下面目录可写（设置为777）
Admin和HOME目录下面的
Cache 
Conf
Temp
Logs

如果需要在后台自动创建项目和项目模块，请设置网站根目录为可写。

后台初始登录帐户名 admin 
密码 admin 
验证码0000 对应的字母

最新版本相对0.9.7版本更新摘要：
	完善模板引擎标签支持（控制更方便）
	增加乐观锁功能
	增加PDO支持（提供了对数据库的最广泛支持）
	多语言支持的改进
	增加URL路由模式支持
	数据库延迟连接，避免额外的连接开销
	惯例配置的支持和更加灵活的配置（简单应用无需定义任何配置文件）
	浏览器缓存的支持（防刷新机制）
	增加数据库字段和Vo属性不同的支持     
	Vo增加create方法（可以快速保存VO对象）
	Dao增加getN方法 （最方便的数据查询方法）
	增加空操作定义 
	Ajax返回在原来的JSON格式外增加XML支持
	Action类增加redirect方法（用于页面跳转）
	完善运行时间显示（执行时间明细）
	App类增加run方法
	Vo自动验证的完善（提供更加灵活的验证）
	基础方法的优化和完善
	更加纯净的Vo对象创建和更新（返回的VO对象不再有多余的特殊属性）
	自动加载路径设置（PHP5）
	修正了很多用户反馈的错误

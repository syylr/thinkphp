先把rbac.sql里面四个表修改一下前缀导入你的数据库

实际应用中有几个地方需要修改

首先当然是_config.php啦，呵呵，不多说
其次是 UserModel.class.php ，这个对应的是用户表，根据需要自己改改
然后是 GroupAction.class.php 第386行，那里面我写了注释，读取用户列表的地方自己酌情修改
(2008-1-22更新：$list=$userDao->findAll('','id,loginid'); 搜索用户表的id和loginid字段，除修改这里以外，在GroupModel里面搜索id,loginid把这里也修改成你需要的字段名称)

换环境后记得每次清空缓存啊，哈哈。

主要是将0.98的RBAC管理模块修改了一下移植到TP1.0的环境

一个比较大胆的想法，自己没有实践过，大家可以试试：
很多时候（比如虚拟主机）不同项目有可能会共用一个数据库，但是使用不同的表前缀来区分项目。如何使用一个RBAC管理模块管理所有项目的权限。
RBAC相关的四个表不要前缀，RBAC配置系统把表前缀赋空值
然后修改TP的RBAC类（Thinkphp\Lib\ORG\RBAC.class.php），把操作那4个表的SQL里面的前缀删除
这样多个项目应该可以共用一个RBAC表了
